{
  "name": "via-lattea-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "La Via Lattea esplorabile: le costellazioni sono i generi dei tuoi progetti, le stelle sono i progetti, i pianeti sono i loro dettagli.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build --base=./",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.182.0"
  },
  "devDependencies": {
    "vite": "^7.2.7"
  }
}
